<%= include '../lib/prototype.js' if ENV['WITH_PROTOTYPE'] || ENV['WITH_BUILDER'] %><%= include '../lib/builder.js' if ENV['WITH_BUILDER'] %>/*  gsa-prototype, version <%= GSA_VERSION %>
if(typeof Prototype == 'undefined')
   throw("prototype.js version > 1.6 is required");
if(typeof Builder == 'undefined')
    throw("script.aculo.us' builder.js library is required");

var Gsa = {};

<%= include 'gsa.js', 'results.js', 'json.js' %>
